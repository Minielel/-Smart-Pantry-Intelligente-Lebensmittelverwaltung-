<!--
Datei: ShoppingListView.xaml

Beschreibung:
Diese XAML-Datei beschreibt das Aussehen einer Seite der App. Hier wird festgelegt, wie die Benutzeroberflaeche aufgebaut ist und welche Steuerelemente angezeigt werden.

Hinweis fuer die Vorstellung:
Wenn man diese Datei erklaert, kann man sagen, dass XAML nur fuer den sichtbaren
Aufbau zustaendig ist. Die eigentliche Logik liegt meistens im passenden ViewModel.
-->

<UserControl x:Class="SmartPantry2.Views.ShoppingListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Haupt-Grid: Zeile 0 = Titel, Zeile 1 = Inhalt. -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Seitentitel – Text kommt aus der Sprachdatei (DynamicResource). -->
        <TextBlock Text="{DynamicResource Title_Shopping}" FontSize="34" FontWeight="SemiBold" Margin="0,0,0,16"/>

        <!-- Inhalts-Grid: Spalte 0 = Einkaufsliste, Spalte 2 = Neuer-Eintrag-Formular. -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="18"/>
                <ColumnDefinition Width="360"/>
            </Grid.ColumnDefinitions>

            <!-- Linke Karte: Alle aktuellen Einkaufslisteneintraege als Kacheln. -->
            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Margin="0">
                <StackPanel>
                    <!-- Pill-Header mit Ueberschrift der Liste. -->
                    <Border Style="{StaticResource PillHeaderBorderStyle}" Margin="0,0,0,14">
                        <TextBlock Style="{StaticResource PillHeaderTextStyle}" Text="{DynamicResource Shopping_ListHeader}"/>
                    </Border>

                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <!-- ListBox: Datenquelle ist Items aus dem ShoppingListViewModel. -->
                        <ListBox ItemsSource="{Binding Items}"
                                 SelectedItem="{Binding SelectedItem}"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                            <!-- WrapPanel: Kacheln fliessen nebeneinander. -->
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>

                            <!-- ItemContainerStyle: Standard-Rahmen entfernen. -->
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="0"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <ContentPresenter/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>

                            <!-- DataTemplate: So sieht jede Einkaufslisten-Kachel aus. -->
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <!-- Kachel: 200x150 px mit Name, Menge, Einheit und Uebernehmen-Checkbox. -->
                                    <Border Style="{StaticResource CardBorderStyle}" Width="200" Height="150" Background="{DynamicResource Surface2Brush}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>

                                            <!-- Zeile 0: Loeschen-Button oben rechts. -->
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <!-- Loeschen-Button; nur sichtbar wenn CanEdit = True. -->
                                                <Button Grid.Column="1" Content="✕" Width="26" Height="26" Padding="0" Background="Transparent" BorderThickness="0"
                                                        Foreground="{DynamicResource MutedTextBrush}"
                                                        Command="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                        CommandParameter="{Binding}">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                            <Style.Triggers>
                                                                <!-- Ausgeblendet wenn kein Bearbeitungsrecht. -->
                                                                <DataTrigger Binding="{Binding DataContext.CanEdit, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="False">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                            </Grid>

                                            <!-- Zeile 1: Name, Menge + Einheit, Uebernehmen-Checkbox. -->
                                            <StackPanel Grid.Row="1" Margin="0,6,0,0">
                                                <!-- Name des Einkaufsartikels. -->
                                                <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>

                                                <!-- Menge und Einheit des Artikels. -->
                                                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                                    <TextBlock Text="{Binding Amount}" FontSize="14"/>
                                                    <TextBlock Text=" " />
                                                    <TextBlock Text="{Binding Unit}" FontSize="14" Foreground="{DynamicResource MutedTextBrush}"/>
                                                </StackPanel>

                                                <!-- Checkbox "In Lebensmittel uebernehmen":
                                                     Ruft AddToFoodCommand auf und uebertraegt den Artikel
                                                     mit dem im Formular gewaehlten Ablaufdatum in die Lebensmittelliste. -->
                                                <CheckBox Content="{DynamicResource Shopping_ToFood}" Margin="0,10,0,0"
                                                          ToolTip="Übernimmt das ausgewählte Einkaufsitem mit dem rechts gewählten Ablaufdatum in Food"
                                                          Command="{Binding DataContext.AddToFoodCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                          CommandParameter="{Binding}">
                                                    <CheckBox.Style>
                                                        <Style TargetType="CheckBox">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                            <Style.Triggers>
                                                                <!-- Ausgeblendet wenn kein Bearbeitungsrecht. -->
                                                                <DataTrigger Binding="{Binding DataContext.CanEdit, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="False">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </CheckBox.Style>
                                                </CheckBox>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </ScrollViewer>

                    <!-- Statusmeldung der Liste (z.B. Fehler beim Uebernehmen). -->
                    <TextBlock Text="{Binding StatusMessage}" Foreground="DarkRed" Margin="0,10,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <!-- Rechte Karte: Formular zum Hinzufuegen eines neuen Einkaufsartikels. -->
            <!-- Nur sichtbar wenn CanEdit = True. -->
            <Border Grid.Column="2">
                <Border.Style>
                    <Style TargetType="Border" BasedOn="{StaticResource CardBorderStyle}">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <!-- Formular ausblenden wenn kein Bearbeitungsrecht. -->
                            <DataTrigger Binding="{Binding CanEdit}" Value="False">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel>
                    <!-- Pill-Header fuer das Hinzufuegen-Formular. -->
                    <Border Style="{StaticResource PillHeaderBorderStyle}" Margin="0,0,0,14">
                        <TextBlock Style="{StaticResource PillHeaderTextStyle}" Text="{DynamicResource Shopping_NewHeader}"/>
                    </Border>

                    <!-- Eingabefeld: Name des Artikels. -->
                    <TextBlock Text="{DynamicResource Label_Name}"/>
                    <TextBox Text="{Binding Name}" Margin="0,4,0,10"/>

                    <!-- Eingabefelder: Menge und Einheit nebeneinander. -->
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock Text="{DynamicResource Label_Amount}"/>
                            <TextBox Text="{Binding Amount}" Margin="0,4,0,0"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" IsEnabled="{Binding CanEdit}">
                            <TextBlock Text="{DynamicResource Label_Unit}"/>
                            <!-- Einheiten-Auswahl: Liste kommt aus dem ViewModel. -->
                            <ComboBox ItemsSource="{Binding Units}" SelectedItem="{Binding SelectedUnit}" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Grid>

                    <!-- Ablaufdatum fuer die spätere Uebernahme in die Lebensmittelliste. -->
                    <TextBlock Text="{DynamicResource Shopping_ToFoodExpiry}" Margin="0,14,0,0"/>
                    <DatePicker SelectedDate="{Binding MoveToFoodExpiryDate}" Margin="0,4,0,12"/>

                    <!-- Hinzufuegen-Button ruft AddCommand im ShoppingListViewModel auf. -->
                    <Button Content="{DynamicResource Btn_Add}" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding AddCommand}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>