<!--
Datei: MainWindow.xaml

Beschreibung:
Diese XAML-Datei gehoert zur Grundstruktur der Anwendung und legt wichtige Oberflaechen oder Ressourcen fest.

Hinweis fuer die Vorstellung:
Wenn man diese Datei erklaert, kann man sagen, dass XAML nur fuer den sichtbaren
Aufbau zustaendig ist. Die eigentliche Logik liegt meistens im passenden ViewModel.
-->



<Window x:Class="SmartPantry2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="SmartPantry" Height="700" Width="1100"
        Background="{DynamicResource BgBrush}">

    <Grid Margin="18">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="18"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        
        <Border Grid.Column="0" Background="{DynamicResource SidebarBrush}" CornerRadius="26" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="{DynamicResource App_Title}" Foreground="White" FontSize="24" FontWeight="SemiBold" Margin="6,4,6,16"/>

                
                <StackPanel Grid.Row="1">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoggedIn}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>

                    <Button Style="{StaticResource NavButtonStyle}" Content="{DynamicResource Nav_Dashboard}" Command="{Binding NavigateDashboardCommand}"/>
                    <Button Style="{StaticResource NavButtonStyle}" Content="{DynamicResource Nav_Food}" Command="{Binding NavigateFoodCommand}"/>
                    <Button Content="{DynamicResource Nav_Recipes}" Command="{Binding NavigateRecipesCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding CanAccessRecipes}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Style="{StaticResource NavButtonStyle}" Content="{DynamicResource Nav_Plan}" Command="{Binding NavigateMealPlanCommand}"/>
                    <Button Style="{StaticResource NavButtonStyle}" Content="{DynamicResource Nav_Shopping}" Command="{Binding NavigateShoppingCommand}"/>
                    <Button Style="{StaticResource NavButtonStyle}" Content="{DynamicResource Nav_Settings}" Command="{Binding NavigateSettingsCommand}"/>
                </StackPanel>

                
                <StackPanel Grid.Row="1" VerticalAlignment="Top">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoggedIn}" Value="False">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <TextBlock Text="{DynamicResource Sidebar_LoginHint}" Foreground="#EFFFFFFF" FontSize="16" Margin="6,10,6,0"/>
                </StackPanel>

                
                <StackPanel Grid.Row="2" Margin="0,10,0,0">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoggedIn}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>

                    <TextBlock Text="{Binding CurrentUserLabel}" Foreground="#EFFFFFFF" TextWrapping="Wrap" Margin="6,0,6,10"/>
                    <Button Style="{StaticResource SecondaryButtonStyle}" Content="{DynamicResource Btn_Logout}" Command="{Binding LogoutCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        
        <Border Grid.Column="2" Background="{DynamicResource SurfaceBrush}" CornerRadius="26" Padding="18">
            <ContentControl Content="{Binding CurrentViewModel}" />
        </Border>
    </Grid>
</Window>
